---
import Layout from '../layouts/Layout.astro';
---

<Layout title="404 — Page Not Found">
  <div class="min-h-[calc(100vh-4rem)] flex items-center justify-center px-4 sm:px-6 lg:px-8">
    <div class="max-w-2xl mx-auto text-center">
      <div class="text-8xl font-bold text-blue-600 dark:text-blue-400 mb-6">404</div>
      <h1 class="text-4xl font-bold mb-4">Page Not Found</h1>
      <p class="text-xl text-gray-600 dark:text-gray-400 mb-8">
        Looks like this page doesn't exist. Maybe it's still being built?
      </p>

      <div class="mb-12">
        <pre class="inline-block text-left bg-gray-900 text-gray-100 p-6 rounded-lg font-mono text-sm">
          <code>{`if (!pageExists) {
  return <Redirect to="/" />;
}`}</code>
        </pre>
      </div>

      <div class="flex gap-4 justify-center">
        <a href="/"
           class="px-8 py-3 bg-blue-600 hover:bg-blue-700 text-white rounded-lg font-medium transition-colors">
          Go Home
        </a>
        <a href="/projects"
           class="px-8 py-3 border border-gray-300 dark:border-gray-700 hover:border-blue-600 dark:hover:border-blue-400 rounded-lg font-medium transition-colors">
          View Projects
        </a>
      </div>
    </div>
  </div>
</Layout>
